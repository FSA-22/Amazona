import PrimaryButton from '@/components/PrimaryButton';

const Home = () => {
  return (
    <main className="flex-center bg-linear-to-r from-secondary via-pink-500 to-primary min-h-screen">
      <PrimaryButton />
    </main>
  );
};

export default Home;
