import PrimaryButton from '@/components/PrimaryButton';

const Home = () => {
  return (
    <main className="flex justify-center items-center bg-linear-to-r from-purple-400 via-pink-500 to-cyan-100 min-h-screen">
      <PrimaryButton />
    </main>
  );
};

export default Home;
